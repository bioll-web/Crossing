---
title: "中断、异常与系统调用"
weight: 4
---

# 中断、异常与系统调用

在 x86 上，这三件事共享同一套硬件机制：**IDT + 特权级切换**。差别只是"谁触发"：

| 类型 | 触发方 | 举例 |
|------|-------|------|
| 中断（interrupt） | 外设（异步） | 时钟中断、键盘 |
| 异常（exception） | CPU 自己（同步） | 缺页、除零 |
| 系统调用 | 用户代码主动 | `int 0x80` |

> 本章笔记待补充。
