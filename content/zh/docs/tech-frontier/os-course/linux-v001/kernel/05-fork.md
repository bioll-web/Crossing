---
title: "fork：一个进程如何分裂成两个"
weight: 5
---

# fork：一个进程如何分裂成两个

`fork()` 是 Unix 最优雅的设计之一。v0.01 的实现非常直接：复制父进程的 `task_struct`、复制页表项（v0.01 还没有 COW）、设置返回值为 0。

重点追踪：

- `copy_process()` 的每个步骤
- 子进程是怎么"被 schedule 选到之后第一次运行"的？
- 为什么父进程看到的 `fork()` 返回值是子进程 pid，子进程看到的是 0？

> 本章笔记待补充。
