---
title: "head.s：跳入 C 语言前的最后一跃"
weight: 3
---

# head.s：跳入 C 语言前的最后一跃

`head.s` 是保护模式下执行的第一段代码，也是 C 语言 `main()` 之前的"最后一个汇编"。它把临时的 GDT/IDT 换成正式版本，搭起分页（把 0~4MB 恒等映射），然后 `call main`。

> 本章笔记待补充。

## 关键点自问

- 为什么要恒等映射？
- 分页开启瞬间，CPU 到底发生了什么？
- 开启分页前后，`jmp` 指令目标地址的含义有什么变化？
