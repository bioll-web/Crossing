---
title: "Lab 10: mmap"
weight: 10
---

# Lab 10: mmap

实现精简版 `mmap` / `munmap`。把文件映射到进程地址空间，延迟加载（缺页时才从磁盘读），修改页在 `munmap` 时写回。

这一 lab 是对前面所有内容的综合应用：缺页处理 + 页表管理 + 文件系统接口 + 进程地址空间 —— 一次全用上。

> 本章笔记待补充。
