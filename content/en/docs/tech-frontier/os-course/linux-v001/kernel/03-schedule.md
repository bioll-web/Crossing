---
title: "进程调度与上下文切换"
weight: 3
---

# 进程调度与上下文切换

`schedule()` 是整个内核最迷人的一百行代码。它的职责只有一个：**从 runnable 的进程里挑一个，然后切过去**。

关键机制：

- 时间片与优先级：`counter` 和 `priority`
- 硬件辅助的上下文切换：x86 的 `TSS` 与 `ljmp`
- 为什么 v0.01 还在用 TSS 切换而现代 Linux 不再用？

> 本章笔记待补充。
