---
title: "setup.s：硬件探测与保护模式切换"
weight: 2
---

# setup.s：硬件探测与保护模式切换

`setup` 阶段是一道分水岭：进去之前是**实模式**（16 位、段 × 16 + 偏移寻址），出来之后是**保护模式**（32 位、GDT/LDT 描述符寻址）。

主要工作：

- 调 BIOS 中断读取硬件参数（内存大小、显卡、磁盘几何）
- 关中断、加载临时 GDT
- 置位 `CR0.PE` 进入保护模式
- 远跳转刷新流水线，落到 `head.s`

> 本章笔记待补充。
