---
title: "Lab 1: Unix Utilities"
weight: 1
---

# Lab 1: Unix Utilities

热身。用 xv6 的系统调用（`fork`、`exec`、`pipe`、`read`、`write`）实现几个小工具：`sleep`、`pingpong`、`primes`（协程筛素数）、`find`、`xargs`。

这一 lab 的重点不在内核，而在让你**熟悉以系统调用为原语的编程模型**。

> 本章笔记待补充。
